# 📦 TODO — Long-Form Reconstruction Engine

## تبدیل Short Puzzle Maker → Documentary Puzzle Studio (8+ دقیقه)

**نسخه:** 1.0 — نقشه راه کامل
**تاریخ:** فروردین ۱۴۰۵
**وضعیت:** آماده بررسی

---

## 🎯 چشم‌انداز کلی

Short Puzzle Maker فعلی → ویدئوهای Shorts (< 60s)
هدف جدید → ویدئوهای Documentary-Puzzle (8–15 دقیقه)

فرمول اصلی تغییر نمی‌کند:

> هر ۴۵ ثانیه = ۱ عکس = ۱ پازل = ۱ فصل از روایت

یعنی ۸ دقیقه ≈ ۱۰ عکس = ۱۰ فصل روایت، بار دوم.

---

## 🏷️ چهار ژنره اصلی

| #   | ژنره                          | شرح                            | مثال موضوع                      |
| --- | ----------------------------- | ------------------------------ | ------------------------------- |
| 1   | **Historical Reconstruction** | بازسازی وقایع تاریخی مهم       | سقوط روم، جنگ جهانی دوم         |
| 2   | **Criminal Casefile**         | بازسازی پرونده‌های جنایی معروف | پرونده Jack the Ripper          |
| 3   | **Lost Civilizations**        | تحقیق و کشف تمدن‌های گم‌شده    | آتلانتیس، پوم‌پیئی              |
| 4   | **Unsolved Mysteries**        | رمزهای تاریخی حل‌نشده          | Nazca Lines، Voynich Manuscript |

---

## 📋 نقشه راه — مراحل کامل

---

### 🟢 مرحله ۱ — پایه‌های معماری (Architecture Foundation)

**هدف:** تغییر مدل ذهنی کل سیستم از "یک پازل" به "یک زنجیره پازل روایت‌دار"

```
TODO-1.1  تعریف فرمت زمانی جدید
    ├── پارامتر: target_duration (8 min / 10 min / 12 min / 15 min)
    ├── پارامتر: images_per_video (بر اساس فرمول: duration / 45s)
    ├── پارامتر: chapters[] (فصل‌بندی روایت)
    └── فایل مرتبط: services/types/serviceTypes.ts

TODO-1.2  تعریف مدل داده فصل (Chapter Model)
    ├── chapter_id
    ├── title (عنوان فصل)
    ├── narrative_text (متن روایت این فصل)
    ├── image_prompt (پرامپت تصویر)
    ├── generated_image_url
    ├── puzzle_config (تنظیمات پازل این فصل)
    ├── duration_seconds (45s default)
    ├── transition_type (چگونه به فصل بعدی بریم)
    └── فایل مرتبط: types.ts

TODO-1.3  تغییر فرض بنیادی سیستم
    ├── از: single_image → به: image_sequence[]
    ├── از: single_puzzle → به: puzzle_chain[]
    ├── از: 60s_recording → به: multi_chapter_recording
    └── فایل مرتبط: hooks/usePuzzleLogic.ts
```

---

### 🟡 مرحله ۲ — موتور روایت (Narrative Engine)

**هدف:** هوش مصنوعی یک داستان ۸–۱۵ دقیقه‌ای کوهن‌ت بسازد، فصل به فصل

```
TODO-2.1  سرویس تولید روایت فصل‌بندی شده
    ├── input: genre + topic + target_chapters_count
    ├── output: Chapter[] (آرایه فصل‌ها با متن و پرامپت هر کدوم)
    ├── ساختار روایت: Hook → Rising Action → Climax → Revelation → Conclusion
    ├── هر فصل باید به فصل بعدی یک "cliffhanger" بذاره
    └── فایل جدید: services/ai/narrativeEngine.ts

TODO-2.2  پرامپت‌های تصویر هر فصل
    ├── هر فصل یک پرامپت مستقل داره
    ├── پرامپت‌ها باید "بصرای متوالی" باشن (cohesive visual story)
    ├── سبک بصری ثابت در طول ویدئو (art style consistency)
    └── فایل مرتبط: services/ai/artGeneration.ts

TODO-2.3  سیستم تایم‌لاین روایت
    ├── نشون دادن "ما الان کجای داستان هستیم" (progress indicator)
    ├── فصل فعلی highlight شده
    └── overlay بصری روی ویدئو (Documentary Timeline Bar)
```

---

### 🔵 مرحله ۳ — موتور تصویر متوالی (Sequential Image Engine)

**هدف:** ۱۰ تصویر تولید کنیم که یک داستان واحد رو نشون بدن

```
TODO-3.1  Batch Image Generation
    ├── فصیله: ۱ به ۱ تصویر به فصل
    ├── هر تصویر قبل از شروع پازل باید آماده باشه
    ├── loading/progress نشون دادن حین تولید
    └── فایل مرتبط: services/ai/artGeneration.ts

TODO-3.2  Visual Consistency Engine
    ├── یک "master style prompt" تعریف کنیم
    ├── این master style به هر فصل اضافه شه
    ├── سبک‌های پیش‌تعریف شده: Cinematic, Dark Documentary, Vintage, Epic
    └── فایل جدید: services/ai/visualConsistency.ts

TODO-3.3  Image Queue Manager
    ├── صف دانلود و cache کردن تصاویر
    ├── pre-loading: وقتی فصل N داره پخش میشه، فصل N+1 باید آماده باشه
    └── فایل جدید: services/imageQueueManager.ts
```

---

### 🟠 مرحله ۴ — موتور پازل متوالی (Chained Puzzle Engine)

**هدف:** هر فصل یک پازل مستقل داشته باشه، ولی انتقال‌ها seamless باشن

```
TODO-4.1  Puzzle Chain Manager
    ├── وقتی پازل فصل N تموم میشه → transition animation → پازل فصل N+1 شروع میشه
    ├── هر پازل می‌تونه complexity مختلف داشته باشه (فصل‌های مهم‌تر = سخت‌تر)
    └── فایل جدید: hooks/usePuzzleChain.ts

TODO-4.2  Chapter Transition Effects
    ├── Fade + Text Overlay ("فصل ۳: شب تاریک")
    ├── Particle Dissolve (قطعات قبلی به ذرات تبدیل شن)
    ├── Timeline Pulse (خط زمانی یک لحظه highlight میشه)
    └── فایل جدید: utils/chapterTransition.ts

TODO-4.3  Adaptive Puzzle Complexity
    ├── فصل ۱ (Hook): آسون‌تر → سریع‌تر شروع میشه
    ├── فصل میانی: متوسط
    ├── فصل Climax: سخت‌تر → هیجان بیشتر
    ├── فصل پایانی: متوسط → پایان آرام
    └── فایل مرتبط: hooks/usePuzzleLogic.ts
```

---

### 🟣 مرحله ۵ — موتور صوتی طولانی (Long-Form Audio Engine)

**هدف:** صدا و موسیقی این بار باید ۸–۱۵ دقیقه seamless باشه، نه ۶۰ ثانیه

```
TODO-5.1  Multi-Track Audio Timeline
    ├── Background Ambient: طول کل ویدئو
    ├── Chapter Stingers: صدای کوتاه هر فصل (2-3 ثانیه)
    ├── Climax Music Boost: فصل Climax صدا بالا میشه
    ├── Fade In/Out بین فصل‌ها
    └── فایل مرتبط: utils/audioFade.ts + سرویس جدید

TODO-5.2  Smart Music Discovery برای Long-Form
    ├── ۱ تراک اصلی (ambient, 10+ دقیقه)
    ├── ۱ تراک هیجان‌انگیز (برای فصل Climax)
    ├── ۱ تراک آرام (برای فصل Revelation)
    └── فایل مرتبط: services/ai/musicSelection.ts

TODO-5.3  Narration/TTS Integration (اختیاری، فاز ۲)
    ├── هر فصل یک متن روایت داره
    ├── می‌شه TTS بهش اضافه کرد
    └── فایل جدید: services/ai/narrationEngine.ts
```

---

### 🟤 مرحله ۶ — رابط کاربری و تجربه (UI/UX Overhaul)

**هدف:** سیدبار و اینترفیس باید از حالت "یک پازل" به "یک پروژه documentary" تبدیل شه

```
TODO-6.1  Genre Selector (جایگزین VisionInput فعلی)
    ├── ۴ ژنره قابل انتخاب
    ├── هر ژنره یک ست قالب موضوع داره
    └── فایل جدید: components/sidebar/GenreSelector.tsx

TODO-6.2  Chapter Studio Panel
    ├── نشون دادن فصل‌های تولیدشده
    ├── Preview تصویر هر فصل
    ├── ویراستن متن فصل
    ├── Drag & Drop فصل‌ها
    └── فایل جدید: components/sidebar/ChapterStudio.tsx

TODO-6.3  Production Timeline View
    ├── یک timeline بصری از کل ویدئو
    ├── نشون دادن کجا هر فصل شروع و تموم میشه
    ├── نشون دادن موسیقی و transitions
    └── فایل جدید: components/ProductionTimeline.tsx

TODO-6.4  Duration & Chapter Count Controls
    ├── slider: طول ویدئو (8 / 10 / 12 / 15 دقیقه)
    ├── auto-calculate: تعداد فصل‌ها
    └── فایل مرتبط: components/sidebar/ComplexityConfig.tsx
```

---

### 🔴 مرحله ۷ — موتور ضبط و صادرات (Recording & Export)

**هدف:** ضبط ۸–۱۵ دقیقه seamless، نه فقط ۶۰ ثانیه

```
TODO-7.1  Long-Form Recording Engine
    ├── فعلی: ۶۰ ثانیه → جدید: 8–15 دقیقه
    ├── Chunk-based recording (هر فصل جداگانه ضبط، بعد merge)
    ├── Progress bar حین ضبط
    └── فایل مرتبط: components/RecordingSystem.tsx + hooks/useMediaRecorder.ts

TODO-7.2  Chapter-Aware Export
    ├── هر فصل یک Marker داره در ویدئو خروجی
    ├── Thumbnail: حالا می‌شه ۱۰ Thumbnail از هر فصل گرفت
    └── فایل مرتبط: components/ThumbnailGenerator.tsx

TODO-7.3  SEO Pack جدید برای Long-Form
    ├── Title: "The Full Story of [Topic] — [X] Minutes"
    ├── Description: فصل به فصل خلاصه
    ├── Tags: genre-specific + topic-specific
    └── فایل مرتبط: components/engagement/MetadataStudio.tsx
```

---

### 🟢 مرحله ۸ — Auto-Pilot جدید (Neural Auto-Pilot V6)

**هدف:** فرایند تولید ۸–۱۵ دقیقه ویدئو کاملاً خودکار

```
TODO-8.1  Long-Form Pipeline
    ├── Step 1: Genre + Topic Selection (خودکار از trends)
    ├── Step 2: Narrative Generation (۱۰ فصل)
    ├── Step 3: Batch Image Generation (۱۰ تصویر)
    ├── Step 4: Music Discovery (۳ تراک)
    ├── Step 5: Assembly & Recording
    ├── Step 6: Export (Video + Thumbnails + SEO)
    └── فایل مرتبط: components/sidebar/NeuralAutoPilot.tsx

TODO-8.2  Pipeline Progress Dashboard
    ├── نشون دادن مراحل پیش‌رفت
    ├── ETA نشون دادن
    └── فایل مرتبط: components/sidebar/PipelineStatus.tsx
```

---

### 🔵 مرحله ۹ — Outro و Engagement (Long-Form نسخه)

**هدف:** Outro طولانی‌تر، Chapter Cards، و Mid-Video Hooks

```
TODO-9.1  Mid-Video Engagement Hooks
    ├── هر ۲–۳ فصل یک "آیا می‌دونستید؟" overlay
    ├── "Keep watching..." hook قبل از فصل Climax
    └── فایل جدید: components/puzzle/MidVideoHooks.tsx

TODO-9.2  End Cards جدید
    ├── Chapter Summary Cards (خلاصه هر فصل)
    ├── ACK & SUB دکمه‌ها
    └── فایل مرتبط: components/puzzle/EngagementCTA.tsx
```

---

## 📂 اسنادی که باید بفرستیم یا بسازیم

| #   | سند                        | وضعیت         | نیاز              |
| --- | -------------------------- | ------------- | ----------------- |
| 1   | `PRODUCT_OVERVIEW.md`      | ✅ دریافت شد  | —                 |
| 2   | `types.ts`                 | ❌ نیاز داریم | مدل داده فعلی     |
| 3   | `serviceTypes.ts`          | ❌ نیاز داریم | تایپ‌های سرویس    |
| 4   | `usePuzzleLogic.ts`        | ❌ نیاز داریم | منطق فعلی پازل    |
| 5   | `NeuralAutoPilot.tsx`      | ❌ نیاز داریم | پایلوت فعلی       |
| 6   | `artGeneration.ts`         | ❌ نیاز داریم | سرویس تصویر فعلی  |
| 7   | `PuzzleCanvas.tsx`         | ❌ نیاز داریم | رندرینگ فعلی پازل |
| 8   | `RecordingSystem.tsx`      | ❌ نیاز داریم | سیستم ضبط فعلی    |
| 9   | `Sidebar.tsx`              | ❌ نیاز داریم | ساختار سیدبار     |
| 10  | `musicSelection.ts`        | ❌ نیاز داریم | سرویس موسیقی      |
| 11  | `storyArc.ts`              | ❌ نیاز داریم | سرویس روایت فعلی  |
| 12  | `useProductionPipeline.ts` | ❌ نیاز داریم | پایپلاین فعلی     |
| 13  | `App.tsx`                  | ❌ نیاز داریم | ساختار کل اپ      |
| 14  | `package.json`             | ❌ نیاز داریم | dependencies      |

---

## 🎯 پیشنهاد ترتیب فرستادن اسناد

```
Round 1 (پایه):
  → types.ts
  → serviceTypes.ts
  → package.json
  → App.tsx

Round 2 (منطق اصلی):
  → usePuzzleLogic.ts
  → PuzzleCanvas.tsx
  → artGeneration.ts

Round 3 (پایپلاین و خودکاری):
  → NeuralAutoPilot.tsx
  → useProductionPipeline.ts
  → storyArc.ts
  → musicSelection.ts

Round 4 (ضبط و صادرات):
  → RecordingSystem.tsx
  → Sidebar.tsx
```

---

## ⚡ خلاصه آنچه تغییر می‌کند

| جنبه        | فعلی (Shorts) | جدید (Long-Form)                |
| ----------- | ------------- | ------------------------------- |
| طول ویدئو   | ۶۰ ثانیه      | ۸–۱۵ دقیقه                      |
| تعداد تصویر | ۱             | ۱۰–۲۰                           |
| ساختار      | یک پازل       | زنجیره فصل‌های روایت‌دار        |
| موسیقی      | ۱ تراک کوتاه  | Multi-track timeline            |
| ژنره        | عمومی         | ۴ ژنره تخصصی                    |
| Auto-Pilot  | ۱ مرحله       | ۶ مرحله pipeline                |
| Outro       | ۱۰ ثانیه      | Full end-card + chapter summary |
| SEO         | عنوان + ۵ تگ  | Full long-form SEO pack         |

---

_اول Round 1 رو بفرست، شروع می‌کنیم به تغییر types و ساختار بنیادی._
